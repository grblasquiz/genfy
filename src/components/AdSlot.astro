---
// AdSense slot. Setear PUBLISHER_ID cuando AdSense apruebe el sitio.
const PUBLISHER_ID = ''; // ca-pub-XXXXXXXXXXXXXXXX
const isDev = import.meta.env.DEV;

export interface Props {
  slot?: string;
  format?: 'auto' | 'fluid' | 'rectangle' | 'horizontal' | 'vertical';
  layout?: string;
  responsive?: boolean;
  label?: string;
  variant?: 'inline' | 'sidebar' | 'anchor';
}

const {
  slot = '0000000000',
  format = 'auto',
  layout,
  responsive = true,
  label = 'Ad',
  variant = 'inline',
} = Astro.props;

const enabled = !!PUBLISHER_ID;
---

{enabled && (
  <div class={`ad-slot ad-${variant}`} data-slot={slot}>
    <span class="ad-label">{label}</span>
    <ins
      class="adsbygoogle"
      style="display:block"
      data-ad-client={PUBLISHER_ID}
      data-ad-slot={slot}
      data-ad-format={format}
      data-ad-layout={layout}
      data-full-width-responsive={responsive ? 'true' : 'false'}
    ></ins>
    <script is:inline>
      (window.adsbygoogle = window.adsbygoogle || []).push({});
    </script>
  </div>
)}

{!enabled && isDev && (
  <div class={`ad-slot ad-${variant} ad-placeholder`} data-slot={slot}>
    <span class="ad-label">[AdSlot · {variant} · {slot}]</span>
  </div>
)}

<style>
  .ad-slot { margin: 1.5rem 0; text-align: center; min-height: 100px; }
  .ad-label {
    display: block;
    font-size: 0.6875rem;
    color: var(--text-subtle);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-bottom: 0.375rem;
  }
  .ad-sidebar { min-height: 250px; }
  .ad-anchor {
    position: fixed; bottom: 0; left: 0; right: 0;
    z-index: 50;
    background: var(--bg-card);
    border-top: 1px solid var(--border);
    padding: 0.5rem;
    margin: 0;
  }
  @media (min-width: 768px) { .ad-anchor { display: none; } }
  .ad-placeholder {
    padding: 2rem 1rem;
    border: 2px dashed var(--border);
    border-radius: var(--radius);
    background: var(--bg-soft);
    color: var(--text-subtle);
    font-size: 0.875rem;
  }
</style>
